# 研究进展汇报（导师版·核实优化稿）

**课题**：面向代码变更的 Playwright 端到端针对性回归测试方法研究  
**英文题目**：Change-Aware Targeted Regression Testing for Playwright End-to-End Tests

## 0. 一句话说明

我想解决 CI 中 Playwright E2E 测试的一个实际问题：**全量跑太慢、只跑少了又怕漏、代码变了测试脚本还容易失效**。

本课题的目标是：**给定一次代码变更，自动产出一组“和本次变更相关、能运行、能覆盖变化”的 Playwright E2E 测试集**。已有测试能复用就选择；选中的测试如果因页面变化失效就修复；已有测试覆盖不到的新变化再生成测试。

---

## 1. 为什么要做

现代 Web 项目提交频繁，CI 中每次都全量运行 E2E 测试成本较高。但 E2E 测试又很重要，因为它验证的是用户真实操作路径。问题是：

1. **全量运行贵**：E2E 需要启动浏览器、访问页面、等待异步状态，执行时间和 CI 资源开销都比单元测试高。
2. **人工挑测试不可靠**：开发者很难从一次 code diff 准确判断哪些 E2E 用例受影响。
3. **测试容易过时**：页面结构、文本、locator 或断言一变，测试可能失败；失败不一定代表功能回归，也可能只是测试脚本过时。

所以工程上真正需要的不是“再生成一批测试”，而是一个面向 CI 的闭环：

> **对一次 code diff，只运行必要的 E2E；该修的修，该补的补，最后给出可执行的 targeted E2E 测试集。**

---

## 2. 研究空白

我把相关工作分成三类来看：

1. **回归测试选择 / 变更影响分析**  
   这类方法主要解决“代码变了，哪些测试要跑”，但成熟工作多集中在单元测试、集成测试或代码覆盖层面。Web E2E 测试和源码之间隔着页面、路由、组件、DOM、locator，不能简单把单元测试 RTS 直接套过来。

2. **E2E 测试生成**  
   现有 Web/E2E 生成工作多从需求、页面或用户路径出发，目标是生成可执行测试。但它们通常不直接回答：“这次 diff 改了什么？缺哪条针对性测试？”

3. **E2E 测试修复**  
   现有修复工作主要关注 locator 失效、等待问题、断言变化等。但多数是“测试失败后修测试”，没有把源码 diff 作为核心上下文，也没有服务于一个 targeted 回归测试集的整体可用性。

因此，本课题的切入点是：

> **建立 code diff 到 Playwright E2E 的关联：先选择受影响测试，再修复过时测试，最后为未覆盖的变更生成补充测试。**

---

## 3. 我要具体做什么

### 输入

- 旧版本 `V_old`
- 新版本 `V_new`
- 本次代码变更 `diff`
- 项目已有的 Playwright E2E 测试集

### 输出

- 一组 targeted Playwright E2E 测试：
  - 已选择的相关旧测试；
  - 修复后可运行的旧测试；
  - 为未覆盖变更新生成的测试。
- 每条测试的选择 / 修复 / 生成依据。
- 执行结果、覆盖结果和成本报告。

### 成功标准

- **少跑**：相比全量 E2E，执行数量和时间明显下降。
- **不漏**：受本次变更影响的测试尽量都被选中。
- **能跑**：最终进入 targeted set 的测试必须在新版本上真实执行通过。
- **相关**：生成的测试必须能触达本次 diff，而不是泛泛生成页面测试。

---

## 4. 方法设计

整体流程可以压缩为五步：

### 第一步：建立旧版本覆盖映射

在 `V_old` 上逐条运行已有 Playwright 测试，记录每条 E2E 测试实际触达的源码文件、路由和组件。

作用：给后续选择提供安全基础。也就是先知道“哪些测试过去碰过哪些代码”。

### 第二步：分析本次代码变更

解析 git diff，得到本次变更涉及的文件、函数、组件、路由和 UI 元素。

其中重点做一个 **Semantic UI Diff**：从 JSX/TSX 中抽取 tag、text、role、aria、testId、href、事件 handler 等语义 UI 节点，比较新旧版本，得到 UI 层面的新增、删除和修改。

### 第三步：选择相关 E2E 测试

选择规则是两个信号取并集：

1. **覆盖信号**：旧版本中覆盖过变更文件 / 路由 / 组件的测试。
2. **UI 语义信号**：测试 locator 和本次变更 UI 节点存在文本、testId、role、href 等语义关联。

覆盖信号负责尽量不漏；UI 语义信号负责在覆盖粒度较粗时提高选择精度。

### 第四步：运行、分诊和修复

在 `V_new` 上运行被选中的测试。

- 通过：直接进入 targeted set。
- 失败：判断是疑似真实回归，还是测试脚本过时。
- 如果是 locator、文本或断言过时，则结合 diff、DOM、trace 和 Semantic UI Diff 给出修复候选。
- 修复后的测试必须重新运行通过，才能进入 targeted set。

### 第五步：分析缺口并生成测试

如果本次 diff 中还有未被已选 / 已修测试触达的部分，则进入缺口生成。

生成不是“随便生成 E2E”，而是带约束生成：

- 必须触达本次 diff；
- 必须包含可解释的操作路径；
- 必须有和变化相关的断言；
- 必须在 `V_new` 上真实运行通过。

---

## 5. 核心创新点

本课题不是单独做“测试选择”“测试生成”或“测试修复”，而是把三者放进同一个 code-diff 驱动的闭环里。

最关键的连接机制是 **Semantic UI Diff**：

- 在选择阶段，它把 UI 变更和测试 locator 关联起来；
- 在修复阶段，它为失效 locator 或断言提供候选；
- 在生成阶段，它指出哪些新增 UI 或变化路径还没有测试覆盖。

因此，它解决的是 E2E 场景里的核心断点：**源码 diff 很难直接对应到浏览器中的用户操作和测试脚本**。

---

## 6. 实验怎么做

### RQ1：能否少跑但不漏？

比较本方法和以下基线：

- 全量运行 retest-all；
- 随机选择同规模测试；
- 静态路由 / 组件启发式；
- 只用覆盖映射，不用 Semantic UI Diff。

主要指标：

- `Reduction = 1 - |Sel| / |S|`：少跑多少；
- `Safety = |Sel ∩ Affected| / |Affected|`：受影响测试是否漏选；
- `Precision`：选中的测试中有多少确实相关。

### RQ2：diff 约束生成是否更相关？

比较：

- 无 diff 约束的页面 / 功能驱动生成；
- 本方法的 diff 缺口生成。

主要看：

- 是否触达 diff；
- 是否可执行；
- 人工检查语义上是否是有效测试。

### RQ3：修复是否提升 targeted set 可用性？

比较：

- 不修复；
- 纯属性 self-healing；
- 不带 diff 上下文的 LLM 修复；
- 本方法的 diff + DOM + 语义候选修复。

主要看最终 targeted set 中有多少测试可以实际运行并覆盖变化。

### RQ4：真实项目中成本和泛化性如何？

看 token、迭代次数、wall-clock 时间、CI 单 PR 时延，以及不同 Playwright 项目上的表现。

---

## 7. 当前进展

已经完成一个端到端原型 DiffE2E，覆盖以下能力：

- 旧版本 E2E 覆盖映射；
- diff 分析；
- 双信号测试选择；
- 运行与失败分诊；
- locator / 断言修复；
- 缺口生成；
- 执行验证和结果报告。

### 受控实验结果

在一个受控多文件应用的 24 个真实 git 变更过渡上：

| 方法 | Reduction | Safety | Precision |
|---|---:|---:|---:|
| 本方法 | 0.625 | 1.000 | 1.000 |
| retest-all | 0 | 1.000 | 0.375 |
| random-k | 0.625 | 0.396 | 0.396 |
| 静态启发式 | 0.875 | 0.667 | 1.000 |

这说明在受控主体上，本方法可以在不漏选受影响测试的前提下减少约 62.5% 的执行量。

### 小型真实工程验证

在 React + Vite + Playwright 工程中，Semantic UI Diff 能把覆盖信号的粗粒度选择进一步收窄：覆盖信号会选 3 条测试，UI 语义信号能定位到 1 条更相关的测试，并完成后续修复和新增按钮测试生成。

### 真实数据预实验

对 ReproBreak 相关数据做了离线分析，说明 Semantic UI Diff 对一部分 locator 变化可以直接给出确定性修复线索。不过这里要注意：

- ReproBreak 官方可复现断裂是 449 个；
- 更大规模的记录应表述为 locator changes 或原始候选记录，不能直接叫“真实断裂”；
- 449 个可复现断裂的端到端 Docker 执行验证仍需补齐。

---

## 8. 当前结论

现阶段可以得出的结论是：

1. **问题值得做**：CI 中 E2E 测试确实存在执行成本高、选择困难、脚本易过时的问题。
2. **切入点清楚**：本课题做的是 code diff 到 Playwright E2E targeted set 的自动构造，不是泛泛的测试生成。
3. **方法初步可行**：受控实验和小型真实工程已经跑通，说明“选择 + 修复 + 生成”的闭环可实现。
4. **最终结论还需要真实项目验证**：目前最需要补的是多真实项目 commit replay、真实 LLM 对比、ReproBreak 可复现断裂端到端修复实验。

更稳妥的表述是：

> **已有实验支持该方法的可行性和潜在收益；最终有效性与泛化性需要通过真实项目多 commit replay 和真实 LLM 实验进一步验证。**

---

## 9. 下一步计划

| 优先级 | 任务 | 目的 |
|---|---|---|
| 高 | 跑真实项目多 commit replay | 验证 RQ1 在真实 Playwright 项目上的少跑和不漏 |
| 高 | 接入真实 LLM，并与规则 / stub 对比 | 验证生成和修复是否真正有语义收益 |
| 中 | 完成 ReproBreak 449 个可复现断裂的端到端修复实验 | 补强 RQ3 |
| 中 | 扩大变更类型和样本规模 | 提高统计可信度 |
| 低 | 做 GitHub Actions 集成 demo | 展示工程落地形态 |

---

## 10. 建议向导师强调的三句话

1. **我不是做泛化的 E2E 生成，而是做 code diff 驱动的 targeted E2E 回归测试。**
2. **核心难点是把源码变更、页面语义变化和 Playwright 测试脚本关联起来。**
3. **当前原型已经证明可行，下一步重点是用真实项目和真实 LLM 把有效性做扎实。**

---

# 附录：核实与措辞修改记录

| 原稿表述 | 核实结果 | 建议修改 |
|---|---|---|
| “Playwright 已成主流框架” | 方向上可接受，但最好不要无数据支撑地写“主流”。 | 改为“Playwright 是现代 Web E2E 测试中常用框架之一”。 |
| “传统定位符可致多达 75% 的 Selenium 测试文件每 9 次提交需变更” | 可保留，但它是 ReproBreak 引用的既有实证结果，不是本课题数据。 | 写成“已有实证研究显示……”并给出处。 |
| “脆弱定位符引发 73.6% 的测试断裂” | 可保留，ReproBreak 文献中有该说法。 | 写成“相关研究报告……”更稳妥。 |
| “Meta 披露 14%–22% 软件失效源于过时测试” | 可保留，但我查到的是 UTFix 引用 Meta 相关研究。 | 写成“UTFix 引用的 Meta 研究显示……”。 |
| “4518 仓库研究仅 10.6% 使用 E2E CI” | 该研究对象是 Android instrumentation tests，不是 Web/Playwright E2E。 | 不建议放在主论据；如保留，只能作为“类似端到端测试在 CI 中采用不足的旁证”。 |
| “E2EGit 472 仓库 / 43,670 测试” | 核实正确。 | 可保留。 |
| “ReproBreak（9604 条真实断裂）” | 不准确。ReproBreak 官方报告的是 9,572 locator changes 和 449 个可复现 locator breaks。 | 改为“ReproBreak：449 个可复现 locator break；另有大规模 locator change 记录”。若 9604 是你的内部清洗结果，应说明为“原始候选记录”，不能叫真实断裂。 |
| “现有 RTS/CIA 全在代码/单元层” | 结论方向基本成立，但“全在”过强。 | 改为“成熟工作主要集中在单元 / 集成 / 代码覆盖层面”。 |
| “RQ1 已显著优于全部基线” | 目前主要来自受控实验，真实项目 replay 未完成。 | 改为“受控实验中优于基线，显示出可行性和潜在收益”。 |
| “核心实现风险基本出清” | 过强，因为真实项目 replay、真实 LLM、ReproBreak 端到端验证尚未完成。 | 改为“原型链路已跑通，主要剩余风险在外部效度和真实 LLM 效果”。 |

